import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prestigewedding.app',
  appName: 'Prestige Wedding',
  webDir: 'dist'
};

export default config;
